# Temporal Panic Button 测试点

适用目标：Casualties Unknown Demo + BepInEx/Harmony，可选 KrokMP 联机。联机测试至少准备一台主机和一台客机，并确保双方安装同一份 `TemporalPanicButton.dll`。

## 1. 启动与设置

- 不安装 KrokMP 时启动游戏，能正常进入主菜单，`Settings -> Game` 中能看到本模组设置。测试通过。
- 安装 KrokMP 时启动游戏，主机开房和客机加入都不应在 BepInEx 日志中出现本模组 Harmony/反射错误。测试通过。
- 关闭 `Temporal Panic Button` 总开关后，地雷、炮台、手动按键、投掷物、延迟射击、Dynamite 引线都应回到原版行为。测试通过。
- 开启 `Intelligence Growth Mode` 且智力低于 7 时，地雷、炮台和手动按键都不能触发时停。测试通过。
- 关闭 `Intelligence Growth Mode` 后，时停持续时间和冷却时间应使用设置菜单里的固定值。测试失败。现在设置时停冷却时间失效了，也就是无论设置多少秒的冷却，实际都没有冷却。----已经修复。

## 2. 单人时停基础

- 默认手动键 `V` 只在正常游玩界面触发时停；暂停、交易、容器等菜单打开时不触发。测试通过，后续将新增打开控制台时也不触发。----已经新增并且测试通过。
- 踩地雷时，地雷危险结算前触发时停。测试通过。
- 炮台或枪雷瞄准玩家时，射击伤害结算前触发时停。测试通过。
- 时停期间，发动者可以移动、瞄准、打开医疗 UI、治疗自己。测试通过。
- 时停期间，世界刚体、陷阱、怪物、大部分游戏行为保持冻结。测试通过。
- 时停期间，流血、昏迷恶化、失水等身体状态不继续恶化。测试通过。
- 开启 `Steady Medical Hands` 后，医疗小游戏不受疼痛和意识下降的速度惩罚。测试通过。
- HUD 能正确显示当前时停剩余时间和结束后的冷却。测试失败。同1.5。----已经修复。
- 返回主菜单后，时停特效、HUD、冷却、待释放投掷物、待结算射击、Dynamite 临时状态都被清理。测试通过。

## 3. 单人投掷与射击

- 时停期间投掷物品，物品离手后应悬停在空中。测试通过。
- 时停期间反复投掷或重新瞄准，不应让已悬停物品下落或旋转。测试通过。
- 时停结束后，物品按投掷时记录的方向、速度和角速度继续飞出。测试通过。
- 时停期间玩家开枪，应出现延迟弹道表现，但实际命中/伤害在时停结束后结算。测试通过。
- 炮台触发时停时，炮台射击在时停结束后结算，而不是冻结期间结算。测试通过。

## 4. KrokMP 时停归属

- 主机手动触发时停：主机能动，客机被冻结。测试通过。
- 客机手动触发时停：客机能动，主机视角中客机保持可行动，其他未赋权对象被冻结。测试通过。
- 主机和客机的时停重叠时，两个发动者都能行动。测试通过。
- 一个玩家的时停先结束、另一个玩家的时停还在持续时，先结束者应重新被冻结。测试通过。
- 所有玩家的时停都结束后，世界恢复正常。测试通过。
- 同一个时停广播或回声重复到达时，不应重复触发冷却、重复播放强脉冲或生成重复状态。测试失败。现在结束后的冷却直接失效了，也就是无论设置多少秒的冷却，实际都没有冷却。而且触发时停只会在本地播放时停开始声音，并且只有第一次触发时停会播放声音，比如第一个人触发时停后，他自己能听到时停声音，其他人则是直接暂停，没有时停声音。如果在第一个人的时停期间，其他人使用时停也没有声音。后续希望第一个人触发的时停所有人都能听见时停声音，在时停中则不会。----已经修复且更改。

## 5. KrokMP 炮台打客机回归

- 客机站到炮台或枪雷瞄准线上，主机应把该危险归属给客机并宣布客机时停。测试通过。
- 客机炮台时停期间，客机本地不应受到炮台子弹伤害。测试通过。
- 同一时停期间，主机查看/同步到的客机医疗数据也不应被该炮台子弹扣血。实际上这个不应该这么写，现在客机如果受伤后进行时停，虽然在客机上不会扣血，但在主机视角下客机仍在流血（包括其他状态也在下降，比如疼痛值增加，意识下降），客机的时停结束后也会按照主机结算。----已经修复。
- 客机时停结束后，客机不应被主机权威医疗状态拉回“已受伤”的旧状态。同上一条。----已经修复。
- 允许延迟炮台射击在时停结束后按正常延迟射击规则结算。测试通过。
- 主机自己触发炮台时停的旧逻辑仍然正常，主机冻结期间不受炮台伤害。测试通过。
- 同一炮台在远程抑制窗口内的重复射击，不应生成重复时停。测试通过。

## 6. KrokMP 客机时停投掷物

- 客机触发自己的时停后投掷物品，客机本地物品离手后立即悬停。测试通过。
- 主机视角中，该客机投出的同一物品也应悬停，不应下落或旋转。测试通过。
- 若有第三个客户端，第三方视角中该物品也应在时停期间悬停。未明确第三个客户端是否加载本模组。而且关于第三个客户端是否加载模组的情况，发现一个新的问题。当有一个客户端未加载本模组时，即使他不能拥有时停能力，当他触发炮台和地雷时，其他客户端包括主机还是会显示在他的时停时间内。所以应该加一个是否加载模组的判断，如果这个人没有加载模组，他触发炮台和地雷，不进行处理。----已经修复。
- KrokMP 刚体同步包不应把悬停物品强行改回可运动状态。测试通过。
- 时停结束后，物品在主机和所有客户端按记录的投掷方向、速度和角速度飞出。测试通过。
- 同一次时停中连续投掷多个物品，每个物品都能各自悬停并在结束后释放。测试通过。
- 未获得时停权限的玩家在别人的时停里不应绕过冻结投掷物品。测试通过。

## 7. KrokMP Dynamite 剪短引线

- 手持 Dynamite 时，屏幕右侧出现 `CUT FUSE` 按钮。测试通过。
- 主机点击 `CUT FUSE` 后，该 Dynamite 被标记为已剪短，并同步给客机。测试通过。
- 客机点击 `CUT FUSE` 后，请求发送到主机；主机应用状态并广播给其他客户端。测试通过。
- 已剪短的同一根 Dynamite 下一次使用后，约 `0.1` 秒爆炸，而不是原版 5 秒。测试通过。
- 客机使用 Dynamite 时，即使 KrokMP 通过 `ItemSync.BetterUseItem` 执行远程物品使用，剪短引线仍然生效。测试通过。
- 未剪短的 Dynamite 仍按原版 5 秒倒计时。测试通过。
- 已点燃 Dynamite 的倒计时在时停期间暂停，时停结束后继续。测试通过。
- 已点燃且已剪短的 Dynamite 不应因为 KrokMP 物品同步丢失剪短或剩余时间状态。暂未测试。
- 剪短一根 Dynamite 不应影响另一根 Dynamite。测试通过。

## 8. KrokMP 既有功能回归

- KrokMP 拾取、丢弃、穿戴、背包同步在进出时停后仍正常。测试通过。
- 时停外的普通伤害和医疗同步仍正常。测试通过。
- jumppad、bear trap、coil、spike trap、sound cannon 的 KrokMP 兼容补丁只在时停期间抑制，不影响平时行为。测试通过。
- flashlight、emergency light、rangefinder、jetpack 等 KrokMP 已有自定义物品同步仍正常。测试通过。
- `TemporalPanicButtonAssets` 缺少自定义音效文件时，游戏仍能启动，时停逻辑不受影响。测试通过。

## 9. 构建验证

构建命令：

```powershell
dotnet build TemporalPanicButton.csproj -c Release /p:GameManagedDir="E:\SteamLibrary\steamapps\common\Casualties Unknown Demo\CasualtiesUnknown_Data\Managed" /p:BepInExCoreDir="E:\SteamLibrary\steamapps\common\Casualties Unknown Demo\BepInEx\core"
```

期望结果：`bin\Release\TemporalPanicButton.dll` 生成成功，0 错误。测试通过。

## 10. 本轮修复后回归重点

- 关闭 `Intelligence Growth Mode` 后，把时停冷却设为一个明显值，例如 8 秒。单人触发时停结束前不应提前消耗冷却；时停结束后 HUD 应显示冷却倒计时，冷却结束前地雷、炮台和手动触发都不应再次触发时停。
- KrokMP 中由本机触发时停后，也应在本机时停结束时才开始本机冷却。若别人的时停仍在继续，本机冷却仍应正常倒计时；冷却结束前本机不能再次广播自己的时停。
- 打开游戏控制台时按手动触发键，不应触发时停。关闭控制台后，同一个按键应恢复正常触发逻辑。
- KrokMP 中第一名安装本模组的玩家触发一次新的时停时，主机和所有已安装本模组的客机都应听到时停开始音效；同一个时停仍在进行时，其他玩家叠加触发自己的时停不应再播放新的开始音效。
- 客机触发炮台或地雷时停后，客机本地、主机视角、第三方客机视角中的该客机血量、流血、疼痛、意识等医疗状态都不应在时停期间继续恶化；时停结束后，客机不应被主机权威状态拉回到时停期间恶化后的旧状态。
- 上一条测试完成后，在时停外再次让同一名客机受到普通伤害，主机和客机仍应正常同步医疗数据，确认医疗同步只在时停期间被保护。
- 安装本模组的客机在自己的时停中丢出物品，主机和其他已安装本模组的客机视角中该物品也应悬停；时停结束后应按时停中记录的方向、速度和角速度飞出。
- 房间中存在未安装本模组的客机时，等待主机和已安装本模组的客户端进入关卡至少 2 秒，让模组握手完成。未安装本模组的客机触发炮台或地雷时，主机和其他客机不应进入“属于该未安装客机的时停”；已安装本模组的玩家触发同类危险时仍应正常进入时停。
- 客机剪短 Dynamite 引线后，主机和其他客机应同步剪短状态；该 Dynamite 点燃后仍应约 `0.1` 秒爆炸，未剪短的 Dynamite 仍按原版倒计时。

## 11. 本轮新增回归点

- 多人联机时，`Intelligence Growth Mode` 的实际开关应遵循主机设置。主机开启时，客机本地即使关闭也应按开启处理；主机关闭时，客机本地即使开启也应按关闭处理。
- 多人联机时，客机的时停 HUD 里 `TimeStopReady` 状态应保持精简，不应再显示过多调试信息；进入联机且主机未安装本模组时，客机应提示：`当前主机未加本模组，模组会失效，状态延迟等严重问题`。
- 测试过程中控制台不应再重复输出 `Cannot use 'velocity' on a static body.` 和 `Cannot use 'angularvelocity' on a static body.`。
- 上述三项在单人模式下仍应保持原有行为，不应影响本地设置、HUD 表现和时停逻辑。

## 12. KrokMP 4.0.0 兼容回归点

- 确认 KrokMP 插件目录中的 `KrokoshaCasualtiesMP.dll` 为 `4.0.0` 后启动游戏，BepInEx 日志不应出现 `KrokMP methods missing`、`RegisterServerReceiver`、`RegisterClientReceiver`、`InvalidCastException`、`TargetParameterCountException` 等本模组反射错误。
- 主机和客机都安装本模组时，进入房间后等待至少 6 秒，客机不应显示“当前主机未加本模组”的警告；HUD 的 MP 状态应从 connecting 进入 client/host 一类的可用状态。
- 主机安装本模组、客机安装本模组时，`Intelligence Growth Mode` 仍应完全遵循主机设置，分别测试主机开启/客机关闭、主机关闭/客机开启两种组合。
- 客机手动触发时停、踩地雷触发时停、被炮台瞄准触发时停时，主机应能识别该客机的真实 clientId，不应把客机当成 0 号玩家或未安装模组玩家。
- 客机炮台时停期间，客机本地、主机视角和第三方客机视角中的客机医疗状态都不应继续恶化；时停结束后也不应被主机权威状态拉回到受伤后的旧状态。
- 客机在自己的时停中丢出物品时，主机和其他已安装本模组的客机视角中该物品应悬停；KrokMP 4.0.0 的新物体同步不应把它改回下落或旋转状态。
- 上一条中的物品在时停结束后，应按时停中记录的方向、速度和角速度继续飞出；连续丢多个物品时每个物品都应独立恢复。
- 主机和客机分别测试 `CUT FUSE`：剪短 Dynamite 后，剪短状态应通过 KrokMP 4.0.0 的 `syncId` 正常同步，下一次点燃仍应约 `0.1` 秒爆炸。
- 时停外的 KrokMP 4.0.0 新增同步能力不应被本模组破坏：至少回归拾取/丢弃/穿戴/背包同步、ragdoll 同步、grabber plant 同步、普通医疗同步。
- 开启 KrokMP 4.0.0 的 `Enforce Mod List` 后，若双方模组列表一致，应正常进入并完成上述时停测试；若模组列表不一致，应遵循 KrokMP 自身拦截逻辑，本模组不应绕过或制造额外错误。

## 13. HerobrinesProsthetics 0.8.7 兼容回归点

- 同时安装本模组和 `HerobrinesProsthetics 0.8.7`，不进入 KrokMP 房间，开始单人游戏后确认顶部 HUD 正常显示。
- 保持两个模组默认键位都为 `V` 时，按 `V` 会同时尝试打开 prosthetics 义体 UI 和触发本模组手动时停；记录实际表现。如果义体 UI 打开导致操作不便，应把其中一个模组的按键改成不同键位后继续测试。
- 把本模组 `Manual Time Stop Key` 改为一个不与 prosthetics 冲突的键，例如 `B`。按该键应立即触发手动时停，prosthetics 义体 UI 不应打开。
- 手动时停结束后，HUD 应显示 `COOLDOWN`，倒计时应每秒稳定减少，不应卡在初始值。
- 冷却归零后，再次按手动时停键应能重新触发时停。
- 冷却期间踩地雷或被炮台瞄准，不应绕过冷却再次触发时停；冷却结束后自动触发仍应正常。
- 打开 prosthetics 义体 UI 时，确认本模组冷却仍继续递减；关闭义体 UI 后手动键仍能触发。
- 同时安装本模组、prosthetics 和 KrokMP 4.0.0，主机开房但只有本机一名玩家时，手动时停和冷却递减都应与单人一致。
- 同时安装本模组、prosthetics 和 KrokMP 4.0.0，主机和客机都安装本模组时，主机与客机分别测试手动时停；各自时停结束后各自 HUD 冷却都应递减并最终归零。
- 测试过程中 BepInEx 日志不应出现本模组相关异常；若出现异常，应记录完整日志和复现步骤。
- 测试 prosthetics 的 `Sandivistian` 或其他会改 `Time.timeScale` 的义体能力期间，本模组冷却仍应按真实时间减少，因为冷却使用 `Time.unscaledDeltaTime`。
- 完成以上测试后，单独移除 prosthetics，只保留本模组，再回归一次手动时停、自动触发和冷却递减，确认原行为没有受到影响。
